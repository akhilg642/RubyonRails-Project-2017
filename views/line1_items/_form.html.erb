<%= form_for(line1_item) do |f| %>
  <% if line1_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line1_item.errors.count, "error") %> prohibited this line1_item from being saved:</h2>

      <ul>
      <% line1_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product1_id %>
    <%= f.number_field :product1_id %>
  </div>

  <div class="field">
    <%= f.label :cart_id %>
    <%= f.number_field :cart_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
